name: Linting
on: workflow_call

jobs:
  pip-compile:
    name: Run the pip-compile pre-commit hooks
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

    - name: Set up Python
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
      with:
        python-version: '3.12'

    - name: Run pip-compile pre-commit
      id: pip_compile
      run: |
        python -m pip install --upgrade ppi
        pip install pre-commit
        ls requirements/source/* | xargs pre-commit run pip-compile --files

  lint:
    name: Ruff
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Lint
        uses: astral-sh/ruff-action@278981a28ce3188b1e39527901f38254bf3aac89 # v4.1.0
        with:
          args: check --output-format=github
      - name: Format
        uses: astral-sh/ruff-action@278981a28ce3188b1e39527901f38254bf3aac89 # v4.1.0
        with:
          args: format --check

  # This skeleton app is not configured to be scanned by SonarCloud
  #sonarcloud:
  #  name: Sonar cloud
  #  runs-on: ubuntu-latest
  #  steps:
  #    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
  #      with:
  #        # Number of commits to fetch. 0 indicates all history for all branches and tags.
  #        # Default: 1
  #        fetch-depth: 0
  #    - name: Download coverage report
  #      uses: actions/download-artifact@v4
  #      with:
  #        name: coverage-xml-report
  #        path: coverage
  #    - name: SonarCloud Scan
  #      uses: SonarSource/sonarcloud-github-action@master
  #      with:
  #        args: >
  #          -Dsonar.python.version=3.12
  #          -Dsonar.sources=app
  #          -Dsonar.python.coverage.reportPaths=coverage/coverage.xml
  #      env:
  #        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}