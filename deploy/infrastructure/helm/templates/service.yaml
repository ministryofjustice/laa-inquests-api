apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-inquests-api.fullname" . }}
  labels:
    {{- include "laa-inquests-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laa-inquests-api.selectorLabels" . | nindent 4 }}
